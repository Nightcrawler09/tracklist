%h1
  %i.icon-headphones
  Listing Albums

%br


%table
  %tr
    %th Title
    %th Description
    %th Tag
    %th Author
    %th
    %th
    %th

  - @albums.each do |album|
    %tr
      %td= album.title
      %td= album.description
      %td= tags_links(album)
      %td= link_to album.author.name, author_path(album.author)
      %td
        = link_to album, class: "btn btn-small btn" do
          %i.icon-eye-open
          Show
      %td
        = link_to edit_album_path(album), class: "btn btn-small btn" do
          %i.icon-pencil
          Edit
      %td
        = link_to album, class: "btn btn-small btn", :confirm => 'Are you sure?', :method => :delete do
          %i.icon-trash
          Destroy
%br

= link_to new_album_path, class: "btn btn-small btn-success" do
  %i.icon-hand-up
  New Album
